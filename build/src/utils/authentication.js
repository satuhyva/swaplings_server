"use strict";
// export const authentication = () => {
// }
// const getIsAuthenticationNeeded = (requestPath: string): boolean => {
//     const excludedPaths = ['/login']
//     if (excludedPaths.includes(requestPath)) return true
//     const includedPathRoots = ['']
//     const pathRoot = requestPath.split('/')[1]
// }
